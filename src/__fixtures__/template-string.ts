const contentId = "1";
const url = `/api/content/${contentId}`;
const domain = `http://mysite.com${url}`;

const welcome = "gday";
const person = "bob";
const greeting = `${welcome} there, ${person}`;
